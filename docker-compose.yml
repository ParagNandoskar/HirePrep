services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - ./backend/.env
    environment:
      PORT: 5000
      REDIS_ENABLED: "true"
      REDIS_HOST: redis
      REDIS_PORT: 6379
      NLP_SERVICE_URL: http://nlp-service:8000
      RESUME_SERVICE_URL: http://nlp-service:8000
      AUDIO_SERVICE_URL: http://audio-service:8001
      PYTHON_NLP_SERVICE_URL: http://nlp-service:8000
      PYTHON_AUDIO_SERVICE_URL: http://audio-service:8001
    ports:
      - "5000:5000"
    depends_on:
      - redis
      - nlp-service
      - audio-service
    networks:
      - hireprep-network

  nlp-service:
    build:
      context: ./backend/nlp-service
      dockerfile: Dockerfile
    env_file:
      - ./backend/nlp-service/.env
    environment:
      PORT: 8000
      FLASK_ENV: production
    ports:
      - "8000:8000"
    networks:
      - hireprep-network

  audio-service:
    build:
      context: ./backend/audio-service
      dockerfile: Dockerfile
    env_file:
      - ./backend/audio-service/.env
    environment:
      AUDIO_SERVICE_PORT: 8001
    ports:
      - "8001:8001"
    networks:
      - hireprep-network

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    networks:
      - hireprep-network

networks:
  hireprep-network:
    driver: bridge
